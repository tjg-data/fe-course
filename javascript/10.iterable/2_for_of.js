/**
 * iterable object를 순회하여 값을 가져옴
 * - for(const(let) 변수 of 순회객체) { 실행문; }
 */

/* 배열의 요소를 치환하는 함수 생성 */
function replace() {

}

let fruits = ['🍊','🍋','🍎','🍓', '🍏'];

let obj1 = replace(fruits, '🍎', '🍏');